skip-check:
  - CKV_SECRET_4 # docker-compose contains placeholder dev creds, not real secrets
  - CKV_SECRET_6 # high-entropy strings in docker-compose are placeholder API keys
  - CKV_DOCKER_1 # SSH not exposed, false positive on app/backend Dockerfiles
  - CKV_DOCKER_2 # HEALTHCHECK present but checkov struggles with multi-stage
  - CKV_DOCKER_3 # nginx official image runs master as root, workers as nginx user
  - CKV_DOCKER_5 # update-alone false positive on multi-stage builds
  - CKV_DOCKER_7 # base images use semver tags, not :latest (false positive on alias)
  - CKV_DOCKER_8 # nginx/golang official images by design
  - CKV_DOCKER_9 # apt usage in build stage, not runtime
  - CKV_DOCKER_10 # WORKDIR is absolute, false positive
  - CKV_DOCKER_11 # multi-stage AS aliases are unique within file
