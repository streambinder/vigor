version: "2"
run:
  go: "1.24"
  modules-download-mode: readonly
linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
issues:
  exclude-dirs:
    - templates
  exclude-files:
    - .*_templ\.go$
